# The web framework the alert receiver is built on.
Flask>=3.0

# The production web server that runs the app when deployed. Flask's own
# development server is not built for this and must never face the internet.
gunicorn>=22.0

# HTTP client for talking to Tradier's REST API.
requests>=2.31

# Loads the local .env file during development. On a cloud host there is no
# .env - the host injects the real environment - so it simply does nothing.
python-dotenv>=1.0
